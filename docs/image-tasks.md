# 画像作成タスク（Codex 用）— ゼロベース改訂版

> 最終更新: 2026-06-03。サイトは第4稿（`docs/company-identity.md`）の構成へ刷新済み。
> **旧版（青系 #2b59ff・メディア前面・Hero 右に装飾グラフィック）は全面破棄。** このファイルが現行の単一指示書。
> 画像は Codex で生成 →「配置パス」へ置けば差し替え完了（HTML の `src`/`og:image` 側の修正要否は各タスクに明記）。

---

## 0. いまの画像状況（前提）

**実在・流用中（生成しない／触らない）**

| パス | 用途 | 備考 |
|---|---|---|
| `img/logo02.svg` | ヘッダ・フッタのロゴ、favicon | 墨の筆文字。ブランドの核。差し替え不可 |
| `img/header-bg.jpg` | トップ Hero 背景＋現 OGP | 代表自身が撮った水中写真（深い水・水面の光）。本物。差し替え不可 |
| `img/eto.jpg` / `img/iguchi.jpg` / `img/aoki.jpg` | 会社概要の役員写真 | 実写。差し替え不可 |
| `img/note/kling3-camerawork.png` 他 | トップ note カードのアイキャッチ | note 実記事のアイキャッチ。実物。触らない |
| `img/og/ogp-default.png` | 全ページの OGP/Twitter 共有カード | P1 で生成・配置済み（2026-06-03） |
| `img/now/mark-video.png` / `mark-digest.png` / `mark-genearth.png` | トップ `#now` 3カードの墨スポット | P2 で生成・配置済み（2026-06-03） |

**プレースホルダ・破損はゼロ。** よって以下は「不足の補填」ではなく「ブランドに沿った少数の強化」。無理に数を作らない。

**絶対に作らないもの（旧版の失敗の再発防止）**
- ❌ **Hero に重ねる装飾イラスト/グラフィック** — 写真がブランドを背負う。上に飾りを足さない（identity 明記）。
- ❌ 紫〜青のテックグラデーション、グラスモーフィズム、発光ネオン、粒子・抽象ネットワーク背景。
- ❌ 絵文字アイコン、ストックの「未来都市／回路」、**揃いの汎用3カラムアイコンセット**。
- ❌ 純黒 `#000`／純白 `#FFF` のベタ塗り。

---

## 1. ブランド視覚契約（Codex は色とトーンを発明しない。ここから引く）

**パレット（HEX 厳守）**

| 役割 | 名前 | HEX |
|---|---|---|
| 土台（紙） | 和紙白 | `#F5F1E8` |
| 文字（墨） | 墨 | `#15212B` |
| 基調の闇 | 墨紺 | `#001F3C` |
| 深み（水） | 淵ティール | `#0F3B38` |
| 差し色（熾火） | 熾火アンバー | `#C8822E` |
| 補助 | 鈍色 | `#5C6873` |

- **配分**: 和紙白＋墨の静かな面 70% / 墨紺・淵ティールの暗面 22% / 熾火アンバー **8%以下**（点でだけ。広い面に塗らない）。
- **タイポ**: 見出し＝明朝（Shippori Mincho / Noto Serif JP）、本文＝Noto Sans JP、数字・ラベル＝等幅（IBM Plex Mono）。文字を入れるなら明朝を基調に。
- **質感**: 静けさ・手仕事の墨・誇張のなさ・地に足。筆文字ロゴと血が繋がる「一筆の墨」。アナログの温度（純黒/純白を避ける）。
- **熾火の使い方**: 炎ではなく熾き火。暗面の中に細い線・小さな印・一点だけ、低く長く灯す。

---

## 2. 生成タスク（優先度順・少数精鋭）

### P1 — ブランド OGP（共有カード）✅ 完了（2026-06-03）

> Codex 生成 → `public/img/og/ogp-default.png` 配置済み。全公開ページの `og:image` を同パスへ更新、トップに `twitter:image` も追加済み。

- **目的**: SNS/検索でカード表示されたときの共有画像。現状は水中写真を流用中だが、小サイズでは社名が読めないため、**読める社名入りの静かなカード**を用意する。
- **配置パス**: `img/og/ogp-default.png`
- **サイズ/形式**: 1200×630px（1.91:1）、PNG（~300KB 以下）。
- **Codex プロンプト（方向性）**:
  > 和紙白 `#F5F1E8` の地。中央やや左に、明朝で「合同会社ときしらず」、その下に等幅小文字で「TOKISHIRAZU LLC」。文字色は墨 `#15212B`。下辺または社名脇に、熾火アンバー `#C8822E` の細い罫を一本だけ（画面の 5% 以下）。背景は無地に近く、ごく薄い墨の刷毛のかすれを一筆だけ右下に置いてもよい（淡く、主張させない）。テックグラデ・発光・図形装飾・絵文字は禁止。印刷物のように静かで、余白を効かせる。
- **HTML 反映**: 生成後、`public/index.html` の `og:image` を `https://www.tokishirazu.llc/img/og/ogp-default.png` に変更。他ページ（会社概要・問い合わせ等）にも `og:image` 行を同パスで追加してよい（※この差し替え作業は AI 側で対応可、画像が置かれたら声かけを）。

### P2 —「いま歩いているフロンティア」3カードの墨スポット（任意・ブランド強化）✅ 完了（2026-06-03）

> Codex 生成 → `public/img/now/mark-video.png` / `mark-digest.png` / `mark-genearth.png` 配置済み。各カード先頭に `<img class="card__mark">` を挿入、CSS `.card__mark` を追加（実寸 76px。64px だと等高線が潰れたため調整）。3点とも手描き・内容固有・構図バラバラで、汎用アイコン化は回避できている。

- **目的**: トップ `#now` の3カードは現在テキストのみ。各カードの内容に合わせた**手描きの墨スポット**を一点ずつ添え、note 節（鮮やかな写真）と対比する「静かな墨」の面にする。
- **重要な線引き**: これは禁止された「揃いの汎用アイコンセット」ではない。**3つは構図も筆致もバラバラの、内容固有の一筆**であること。アイコン然とさせない。手で描いた墨の濃淡・かすれを残す。
- **配置パス / 各カードの中身**:
  - `img/now/mark-video.png` — カード01「動画生成AIの効く作り方を検証」: 墨の一筆で描いた**映像のフレーミング枠**（カメラの構図枠＋中心の一点）。
  - `img/now/mark-digest.png` — カード02「映像をショット分解して作り直すハーネス」: 墨で描いた**横長のフィルムを数コマに割った帯**（ショット分解の含意）。
  - `img/now/mark-genearth.png` — カード03「歩き回れる世界を生成で育てる」: 墨の等高線で描いた**小さな地形/球体と、そこを通る一本の小径**。
- **サイズ/形式**: 各 600×600px、**背景透過 PNG**。主線は墨 `#15212B`、和紙白の上に置く前提。熾火アンバーは**一点だけ**（例：中心点や小径の先）入れてよいが無くてもよい。
- **HTML/CSS 反映**: 各カード先頭に `<img class="card__mark" ...>` を追加（`.card__mark { width: 64px; height: 64px; margin-bottom: 1rem; }` 目安）。※実装は画像が揃ってから AI 側で対応。
- **判断**: もし墨スポットが「装飾の繰り返し」に見えるなら**入れない**。余白は埋めるものではなく効かせるもの。P1 を優先。

### P3 — 自社メディア柱記事のアイキャッチ（記事①: genai-journey-timeline）✅ 完了（2026-06-09）

> Codex（imagegen / gpt-image-2）生成 → `eyecatch.png` 配置。`eyecatch.webp` を派生。記事の og:image/twitter:image・冒頭ヒーロー（`.article__hero`）・トップ「メディア」節カードサムネに配線済み。
>
> **改訂（2026-06-09・第3版＝確定方針）**: 記事冒頭の「アイキャッチ」は装飾的な水墨バナーではなく、**記事全体の概要を一目で伝えるサムネ（カバー要約）**にする。絵柄は **HP外枠の墨トーンではなく、本文図 fig01–03 と同じ「日本のカラフルなビジネス向け解説イラスト」調**に統一（ユーザー判断 2026-06-09: 記事のサムネはトップから見て内容が分かることを優先。墨トーンは記事サムネには使わない）。
> - **サイズ**: 1600×900（16:9・本文図と同family）、PNG ~400KB 以下。
> - **内容**: 表紙要約。見出し主役「動画生成AIが、閾値を超えた」＋小さく「2026年2月 ─ 量産から"表現"へ」。閾値超え／量産でなく表現の道具／プロ参入が伝わる。fig03（詳細3カラムまとめ）の丸写しにはせず、すっきり表紙化。主要素は中央寄り（SNSクロップ耐性）。
> - **配線**: 記事 og:image/twitter:image・冒頭ヒーロー（`.article__hero` を 16:9 に戻す）・トップ「メディア」節サムネ。
> - **第2版の墨バナー（1600×640 水墨・閾値の一線）は破棄せず `public/img/banners/sumi-threshold-1600x640.{png,webp}` へ退避**。別ページのキービジュアル候補として温存（未割当）。

- **対象記事**: `/media/articles/genai-journey-timeline/`（「2026年、Seedance 2.0 / Kling 3.0 で動画生成AIが閾値を超えた」）。content から取り込み済み（R-002）。
- **用途（3役）**: ① 記事 `og:image` / `twitter:image`、② 記事冒頭のヒーローバナー、③ トップ「メディア」節のカードサムネイル。1枚で兼ねる。
- **配置パス**: `public/media/articles/genai-journey-timeline/eyecatch.png`（og:image 用・PNG）。ヒーロー/サムネは `eyecatch.webp`（cwebp 派生・軽量）を参照。
- **サイズ/形式**: 1600×900px（16:9）、PNG ~300KB 以下。
- **トーン**: §1 ブランド契約厳守（**HP 外枠の静かな墨トーン**。**本文図 fig01–03 のカラフル路線は持ち込まない**＝register が別。figures-handoff.md 参照）。
- **Codex プロンプト（方向性）**:
  > 和紙白 `#F5F1E8` の地、横1600×縦900。静かな墨 `#15212B` のトーン。画面を低く長く横切る、細い熾火アンバー `#C8822E` の **一線**（この記事の主題「閾値＝越えた一線」の比喩。画面の 8% 以下、発光させず低く灯す）。線の手前は淡い墨のかすれ、線の向こうへ余白がひらける構図。右下にごく薄い墨の刷毛を一筆だけ（淡く主張させない）。任意で、左上に明朝で小さく「動画生成AIが、閾値を超えた」、その下に等幅で「2026.02」を添えてよい（小さく・余白を効かせる／文字が崩れるなら入れない）。テックグラデ・発光ネオン・粒子・グラスモーフィズム・絵文字・純黒/純白のベタ・汎用アイコンは禁止。印刷物のように静かに。
- **HTML 反映（画像が置かれたら AI 側で対応）**: 記事の `og:image`/`twitter:image` を `https://www.tokishirazu.llc/media/articles/genai-journey-timeline/eyecatch.png` へ。記事冒頭にヒーローバナー `<img>`、トップ「メディア」節のカードサムネに `eyecatch.webp`。

---

## 3. 共通ルール

- パレット・タイポは §1 厳守。**色やトーンを発明しない。**
- 人物・第三者の商標・ロゴを含めない（権利配慮）。自社ロゴは `img/logo02.svg` を使い、ラスタ生成で似せて描き起こさない。
- Web 用に圧縮（OGP ~300KB 以下、透過 PNG は軽量に）。
- 生成物のパスは上記どおり厳守（HTML 側を同名で待ち受けられるように）。新規ディレクトリ `img/og/`・`img/now/` は生成時に作成。
- 迷ったら判断基準はひとつ——**「静けさ・手仕事の墨・誇張のなさ・地に足」から外れていないか。** 外れるなら作らない。
