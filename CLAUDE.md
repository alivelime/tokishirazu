# tokishirazu-media

> このファイルは**毎ターン読まれる「現況の一枚」**。薄く保つ。詳細は別ファイルへ逃がす。
> 数値（ファイル数・サイズ等）はここに書かない（すぐ嘘になる）。

## いまどこ

- **これは何か**: `tokishirazu.llc` の企業サイトを、法人運営の信頼ページ＋案件別DRMプレセルLPの配信基盤として整備するメディアプロジェクト。初期構想は `media_site_plan.md`。
- **現フェーズ**: 既存サイト（Start Bootstrap Agency 由来の静的 HTML / SCSS / gulp 構成）の上に、最小の信頼ページと1案件分のLPを小さく検証する段階。まだコード改修には入っていない（計画フェーズ）。
- **次アクション**: `media_site_plan.md` §11 のタスクから着手 — まず既存サイトの技術構成・デプロイ方法・現ページ構成を記録し、必須ページの不足を洗い出す。

> このリポジトリでブラウザ確認が要るときは Playwright MCP が使える（インストール済み Chromium を使用、設定は `.mcp.json`）。本構想の原文は `media_site_plan.md`、退避した旧テーマ README は `README.theme.md`。

## 新しいセッションが最初に読む順

1. `README.md` — 三行サマリ
2. この `CLAUDE.md` — 現況・ルール
3. `STATUS.md` — 現在状態（再開地点）
4. `MISSIONS.md` — 進行中の未完作業
5. `learnings.md` — 既知の落とし穴（同じ失敗を繰り返さない）
6. `decisions/` — 確定方針（ADR索引）
7. 今回のタスクに関係するファイル

> 過去ログ全部は読まない。正本と現在状態を先に読み、必要な箇所だけ掘る。

## どこに何を書くか（配置ルール）

| 種類 | 置き場所 | 性質 |
|---|---|---|
| 現況の一枚 | `CLAUDE.md` | 上書き・薄い |
| 現在状態（再開地点） | `STATUS.md` | 上書き |
| 進行中の未完作業の台帳 | `MISSIONS.md` | active を出し入れ |
| 作業・判断の生ログ | `log.md` | **追記のみ**・時系列 |
| 既知の落とし穴 | `learnings.md` | 追記（`L-NNN`） |
| 確定した不可逆な方針 | `decisions/NNNN-*.md` | **追記のみ・書換禁止** |
| 人間向け説明・設計 | `docs/` | 自由 |

迷ったら: **「いま何が真か」は上書き系（STATUS/CLAUDE）、「いつ・なぜそうしたか」は追記系（log/decisions/learnings）**。
空フォルダを先に作らない。必要になった時点で掘る。

## 運用ルール（このプロジェクトの作業契約）

- **最新状態と履歴を必ず分ける**: 結果は上書き、経緯は追記。混ぜない。
- **会話内で完結させない**: まとまった作業の節目で `STATUS.md` を更新し `log.md` に1行残す。
- **未完作業は `MISSIONS.md` の `## active` に1行足す。完了（コミット）で archive に落とす。**
- **失敗したら `learnings.md` に `L-NNN` で1件足す**（次回回避ルールを命令形で）。
- **確定方針は ADR に落とす**。コードや本文に方針を埋め込まない（単一正本）。
- **自動 / 確認の線引き**: 可逆かつ局所な変更は自動でよい。不可逆または大局的な変更は人間確認を待つ。
<!-- SELFMOD:START -->
- **ハーネス自己改変は禁止（policy: strict）**: `.claude/`・hooks・本CLAUDE.mdの構造ルール・役割定義を AI が勝手に書き換えない。設計変更は**提案で止めて人間がレビュー・適用**する。ADR の promote（Proposed→Adopted）も人間の操作。日々のタスクで触ってよいのは成果物・STATUS・log・learnings・MISSIONS だけ。
<!-- SELFMOD:END -->
- **続きをやってと言われたら**: 上の読む順で現状把握 → `STATUS.md` の「次アクション」から再開。情報が足りなければ**勝手に推測して書き始めず聞く**。

## このプロジェクトが避けたい失敗モード（先に言語化）

| # | 失敗 | 構造的対策 |
|---|---|---|
| F1 ソロ判断 | 重要判断を1人で即決して外す | 高リスク地点は独立サブエージェントで相互確認 |
| F2 手順未エンコード | 毎回やり方が違う | 反復作業はスキル/手順書に決定的に書く |
| F3 目的喪失 | 長くなって当初目的を見失う | STATUS/MISSIONS で目的を持ち回す |

> ハーネス自体を変更するときは `docs/harness-review-rubric.md` の8項目で自己レビューする。

<!-- PROFILE:START -->
## プロファイル: code（コード開発）

このプロジェクトはコードを書く。上の共通ルールに加えて:

### 納品ゲート（実装変更のたび）
- 変更後に **対象テスト / lint / 型検査** を走らせてから完了とする。
- 緑にならない変更は完了扱いにしない。落ちたら出力をそのまま報告する。
- 計画に無いリファクタ・抽象化・他バグの同時修正をしない（過剰実装禁止）。

### Git 納品ルール
- 作業前に `git status --short` を確認。ユーザー/他エージェントの無関係な変更を戻さない。
- 関係するファイルだけ `git add`。1つの意味のある変更単位でコミット。
- push 前にブランチ・リモート・差分を確認。push と PR は方針次第で人間承認。
- デフォルトブランチでの直接作業を避け、ブランチを切る。

### テスト規律（アンチ・ホローテスト）
- フィクスチャは実データのコピー。期待値は人手で導出（実装出力のコピペ禁止）。
- assertion は具体値で（`toBeDefined()` 単独不可）。境界だけモック、ロジック本体は実物を通す。
- 時々わざとコードを壊し、テストが落ちることを確認する。

### 推奨（必要になったら足す）
- 速い fail-fast oracle（build / unit test）を PostToolUse hook で自動実行。重いE2Eは手動。
- `code-reviewer` サブエージェントで diff を独立レビューしてからコミット。
<!-- PROFILE:END -->
